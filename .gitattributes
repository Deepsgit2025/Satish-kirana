# Keep line endings identical on every machine. The store server is Linux, the
# dev machines are Windows; migration checksums and Prettier both care.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.ttf binary
*.otf binary
*.woff binary
*.woff2 binary
