{
  "name": "ssbazar",
  "version": "0.0.0",
  "private": true,
  "description": "SS Super Bazar - offline-first supermarket POS, inventory and back office",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "workspaces": [
    "packages/*",
    "server",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "clean": "tsc -b tsconfig.build.json --clean",
    "typecheck": "tsc -p tsconfig.json && tsc -p apps/office/src/renderer/tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "db:migrate": "node --env-file-if-exists=.env --import tsx server/src/db/migrate.ts",
    "db:reconcile": "node --env-file-if-exists=.env --import tsx server/src/reconciliation/run.ts",
    "backup:run": "node --env-file-if-exists=.env --import tsx server/src/backup/backup-cli.ts",
    "backup:verify": "node --env-file-if-exists=.env --import tsx server/src/backup/verify-cli.ts",
    "catalogue:import": "node --env-file-if-exists=.env --import tsx server/src/catalog/import-cli.ts",
    "dev": "node -e \"console.log('No runnable app yet. See docs/build-order.md - step 0 is the skeleton.')\"",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm run test",
    "office:build": "npm run build -w @ssbazar/office",
    "office:start": "electron apps/office"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.1.0",
    "electron": "^43.4.1",
    "eslint": "^10.8.1",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.6",
    "tsx": "^4.23.12",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  }
}
