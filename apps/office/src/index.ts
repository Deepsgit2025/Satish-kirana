/**
 * Entry point for the office app.
 *
 * Deliberately empty: step 0 is the skeleton only. Electron, React and the
 * local SQLite cache arrive with the first screen (see docs/build-order.md).
 */

export {};
