{
  "name": "@ssbazar/counter",
  "version": "0.0.0",
  "private": true,
  "description": "Billing terminal (Electron) - runs on each counter, offline-first",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b tsconfig.build.json"
  },
  "dependencies": {
    "@ssbazar/shared": "*"
  }
}
