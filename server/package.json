{
  "name": "@ssbazar/server",
  "version": "0.0.0",
  "private": true,
  "description": "Postgres access, API, sync and backup jobs",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "db:migrate": "node --env-file-if-exists=../.env --import tsx src/db/migrate.ts"
  },
  "dependencies": {
    "@ssbazar/shared": "*",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1"
  }
}
